# notekeeper

A tiny local notes CLI I share with my study group. Notes are stored as
plain text files under `~/.notekeeper/`.

Run: `python main.py add "buy milk"` / `python main.py list`

Design notes live in `docs/NOTES.md`.
