# Ledger Triage Fixture

This synthetic project already has a crowded `BLINDSPOT_LEDGER.md`.

Run target:

```text
Use $blindspot-audit mode: ledger-triage on this project. Generate a decision board if there are many choices.
```

The project is intentionally tiny. The ledger is the test surface.

