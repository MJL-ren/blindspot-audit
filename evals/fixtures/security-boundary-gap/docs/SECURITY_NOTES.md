# Security Notes

## Closed in June

A deploy credential was accidentally placed in `settings.py` during the first
preview. I removed the value from the current file, so I marked the incident
closed. The credential value is not included in this fixture.
